[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan migrate --force --no-interaction && php artisan storage:link --force 2>/dev/null; php -S 0.0.0.0:$PORT -t public"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
